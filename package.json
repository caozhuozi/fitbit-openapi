{
  "name": "fitbit-openapi",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "redocly lint openapi.yaml",
    "bundle": "mkdir -p dist && redocly bundle openapi.yaml --output dist/fitbit-openapi.yaml",
    "validate": "npm run lint && npm run bundle"
  },
  "devDependencies": {
    "@redocly/cli": "^1.25.15"
  }
}
